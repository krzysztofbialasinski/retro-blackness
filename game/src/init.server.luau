--!strict
-- Placeholder in-game server script. This `game/` tree is what the publish pipeline builds
-- with `rojo build` and ships to a Roblox experience via Open Cloud. Replace with real game
-- logic (this backend service is what the game talks to over HTTP).

print("retro-blackness game code loaded")
